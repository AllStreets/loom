# LOOM — session handoff (updated 2026-09-04)

*Started by Claude Fable 5, continued by Claude Opus 4.8, for the next model taking over. Read this, then `README.md` (roadmap table), `docs/BRAND.md`, and `docs/FOLLOWUPS.md`. Repo: `~/Downloads/LOOM`, remote `github.com/AllStreets/loom`, everything below is merged to `main` and pushed.*

> **Round-1 review (2026-09-04, Opus 5).** Four adversarial reviewers plus a real `npm run tauri build`. The build itself was the biggest finding: **it failed outright** (Tauri defaults the macOS deployment target to 10.13; whisper.cpp needs 10.15+ — set `bundle.macOS.minimumSystemVersion`), and once it built **the app died at launch** on `@rpath/libonnxruntime` with no LC_RPATH — in dev too, so LOOM had not run at all in its current state (fixed with rpaths in `build.rs` + the dylibs as `bundle.macOS.frameworks`). Other confirmed-and-fixed: `build.rs` watched only `.git/HEAD`, which never changes when you commit on a branch, so every reweave after the first would have baked a stale sha; the swap replaced the running executable BEFORE arming the sentinel that heals it (a brick path); the organ timeline shares its work tree with loomhome and staged `*`, so one organ write after threading would have committed the vendored crates and every shelved binary; threading could never complete in dev; `npm` cannot run from a Finder-launched app (it is a `#!/usr/bin/env node` shim and GUI PATH lacks node) — every npm spawn now carries a PATH with the recorded node; the streaming runner's timeout never fired while a child kept printing, so the long jobs had no timeout; an organ could reach the binary swap through `loom.self` behind a one-click pill (now it may only *ask*, and shell-owned protected chrome does the asking). **Open decision for the next model:** TS validation still runs model-authored vitest while Rust runs `--no-run`; see `docs/FOLLOWUPS.md` §Round-1 review. **Rule this taught: build it and launch it before calling it shipped.**

> **Update 2026-09-04 (Phase 23 — Rebirth):** Two things happened on branch `phase-23-rebirth`. **(1) The second excision (23a):** the Cockpit is gone by owner verdict — decks, watch/salience, market engine, cloud builder, deckserve, bundled AUSPEX/EMBER snapshots. NEVER reintroduce them. One brain (local). Four organ powers (`timeline · voice · notify · pulse`). One initiative archetype (morning brief). **(2) Packaged self-rebuild, offline** — spec `docs/superpowers/specs/2026-09-02-rebirth-design.md`, plan `docs/superpowers/plans/2026-09-02-rebirth.md`. Vocabulary: *genome* (the git bundle shipped in the app via `scripts/genome-bundle.mjs`, resource `src-tauri/genome/`), *loomhome* (`<app_data>/loom/` — `source/`, `vendor/`, `target/`, `worktrees/`, `generations/<sha>/loom`, `threads.json`, `generations.json`, `reweave.json`, `warden.json`, `recovery.json`), *threading* (`threads.rs` — tool discovery from fixed candidate dirs, the resumable ceremony seed → deps → vendor → warm → register → stamp; needs the network ONCE, says so), *reweave* (`reweave.rs` — assets → core → stage → swap → relaunch, cancellable until swap, one global `exec::JOB` slot shared with threading), *generations* (`generations.rs` — ledger, prune never drops current/previous), *platform* (`platform.rs` — pure `swap_plan` + macOS `execute`, write-then-rename, ad-hoc codesign), *the warden* (`warden.rs` — the PREVIOUS generation's binary runs `--warden <job>` dispatched as the first statement of `run()`, watches the sentinel, heals + relaunches on crash/timeout; pre-main backstop in `preboot_heal` when no warden pid is alive). Modes: `tauri::is_dev()` → dev (source = cwd, reweave builds but never swaps), else packaged (source = loomhome/source). `build.rs` bakes `LOOM_GENOME_SHA`. Validation: `npx` is GONE — worktrees symlink `node_modules`, tsc/vitest run through the recorded `node`; cargo always `--offline`; packaged worktrees share `loomhome/target`. TS: `src/lib/loom/reweave.ts` + `generations.ts` (PROTECTED), the reweave card (`chrome/Reweave.tsx`), packaged framing on `KernelDiff`, healed-generation notice, companion rules (reweave / thread / identity / return) with consent cards, Shuttle entries, Settings → LOOM page (behind a kernel-only `self` power), Tapestry generation knots. Sentinel `armedBy: "reweave"` ownership rule: a `booting` armed by reweave with a live warden pid is the WARDEN's — pre-main must Leave it. If extending: NEVER add a network call to reweave; NEVER let a new safety file stay editable (the enumerated test in `kernel.rs` is the guard — every new rebirth module is in `PROTECTED_RUST`, plus `.cargo/config.toml` at any depth and the two protected TS prefixes); the warden's `decide` table is a unit test — re-derive before touching. Owner-verified vs CI-proven is stated honestly in README. Rate limits interrupted several subagents mid-task this run; each resumed from its worktree — see the memory dir for the recovery pattern.

> **Update 2026-08-28:** Phases 20 (Initiative), 21 (Selfhood), AND 22 (Marrow) are all SHIPPED. LOOM edits its own TypeScript kernel (21) AND its own Rust core (22) behind five walls (isolation → validation → owner diff-approval → commit → recovery), self-protection enforced in Rust (`src-tauri/src/kernel.rs` `is_editable` + PROTECTED/PROTECTED_PREFIXES/PROTECTED_RUST — it cannot edit its own safety machinery, enumerated test). **Phase 22 specifics:** Rust core (`src-tauri/src/**/*.rs` minus the safety core + Cargo manifests + guard script), dev-mode; validated with `cargo check` + `cargo test --no-run` (NEVER execute untrusted generated tests — RCE); apply = commit + "restart to load" (Rust can't hot-reload — `tauri dev` doesn't watch src-tauri). The recovery gap (a compile-clean Rust edit that panics before `boot_recover` runs) is CLOSED by `scripts/kernel-preboot.mjs` (pre-compile guard in `beforeDevCommand`) + `preboot_heal` (pre-`main` hook) + a source-root sentinel mirror `.loom-boot.json` (gitignored). State machine has an `armedBy` field: guard arms `applied`→`booting`, heals `booting`; pre-main is the guard-absent backstop; good edits survive restart 1, bad edits heal on restart 2. The next horizon is **Phase 23** — packaged self-rebuild (binary swap + toolchain distribution — big, platform-specific, deferred; see `docs/FOLLOWUPS.md` Phase-23 backlog). Live Rust self-edit + restart + heal is owner-verified in dev, NOT headless-CI-provable; CI proves whitelist/self-protection, real-cargo validation, the guard/pre-main rollback logic + sentinel state machine. TWO adversarial review rounds; round 2 returned clean ("merge approved").

## The vision

LOOM is Connor's flagship: **a sovereign computer that is its own developer.** You describe a capability in a sentence; a local model plans it, writes the code *and its tests*, proves it in a sandbox, repairs its own failures, commits to a git timeline, and asks permission before the new "organ" runs — fully offline, no cloud, no keys, yours forever. Tauri v2 (Rust core) + React/TS/Vite shell + Ollama model fleet. The kernel/organ split is load-bearing: the kernel is protected; organs are model-written modules behind permission grants.

Connor's standing verdict (be governed by this): **no decorative widgets, no copied wrappers, no fake-working surfaces.** Before adding anything ask "could any Electron wrapper ship this?" — if yes, root it in what only LOOM has: its own git history, self-built organs, learned salience weights, one voice/⌘K grammar. Honesty is brand law (`docs/BRAND.md`): calm lowercase copy, panels state *why* they're empty, budgets/limits visible, never overclaim.

## Where we are (phases 15–19 shipped this run)

- **15 · Command** — (AGORA launch control; later removed, see 18) + learned-weights inspection in the WatchPanel, whisper log silence.
- **16 · Identity** — the brand system: woven-monogram glyph everywhere (`public/brand/`), BRAND.md; **the Tapestry** (constellation deleted; LOOM's git/organ/build history rendered as interlaced woven cloth behind the orb — `src/lib/tapestry/`, geometry matters: curves, never straight grid lines, Connor rejected the first grid render); **the Shuttle** (⌘K palette + voice share ONE command catalog derived from the real rule tables, drift is test-enforced — `src/lib/shuttle/`).
- **17 · Depth** — the Terminal was empty because the Rust proxy sent no User-Agent (Yahoo 429s without one — fixed, live-verified). Typed keyless market engine `src-tauri/src/market.rs`: Yahoo charts, Coinbase Exchange (ticker/book/trades), Frankfurter FX; hardcoded hosts, validated symbols. Terminal: editable watchlist, symbol detail charts, crypto/FX strips, per-source health chips.
- **18 · Excision** — **AGORA removed entirely by owner verdict. Never reintroduce it.** The Coinbase order-book floor survived and lives in the Terminal (click a crypto-strip symbol → `src/components/terminal/Floor.tsx` overlay). Four decks: void · globe · terminal · ember.
- **19 · Vigor** — organs got six real powers behind the `need()` grant seam: `market · watch · timeline · voice · notify · pulse` (`src/lib/organs/api.ts`, budgets in `budgets.ts`, toasts in `chrome/Notices.tsx`). Manifest declares `powers`, permission card lists them plainly, revocable live from the organ title bar, token-bucket budgets with a THROTTLED chip, deterministic sandbox mocks so generated tests are grounded (`loom.notify.sent`, `loom.voice.said`, `loom.pulse.registered`). Builder prompts teach the APIs conditionally (`requestImpliesPowers`) with a BTC-5%-drop few-shot. "Alert me when BTC drops 5%" now builds a real, running tool.

Gate state at handoff: `npm run check` fully green — **1218 vitest + 212 cargo** (1 ignored live test) on `phase-23-rebirth`.

## Why: Connor said "this still just isn't that impressive"

Diagnosis he agreed with: the surfaces got polished but the revolutionary loop had a toy ceiling and zero initiative. He chose the maximal path — **all three** of powers, initiative, and kernel self-modification. Phase 19 (powers) is done. Two remain:

## Where we're going

**Phase 20 — Initiative (SHIPPED 2026-08-26).** LOOM observes usage and proposes organs unprompted. `src/lib/initiative/`: `observe.ts` (passive usage ledger `loom.usage.v1`, subscribes to existing events, no polling), `propose.ts` (pure deterministic rules engine — three earned archetypes morning-brief/price-alert/topic-digest, hard evidence gates, returns null as the common case), `store.ts` (`loom.initiative.v1` rate-limit + never-list), `runtime.ts` (debounced event-driven emit). `src/components/chrome/Proposal.tsx` = the calm card (weave it / not now / never); "weave it" dispatches the SAME `loom-utterance {..., initiative:true}` a typed build uses → normal pipeline + permission card. Setting `cockpit.initiative` (default on). Design principle enforced throughout: **earned, not guessed** — every proposal quotes real observed counts; a rule that can't must not fire (the review caught and we fixed a morning-brief path that would've fired on an empty watchlist). If extending: add archetypes as rules with hard evidence gates, keep detection model-free.

**Phase 21 — Selfhood (SHIPPED 2026-08-26).** LOOM edits its own TS kernel. `src-tauri/src/exec.rs` (hardened fixed-argv spawn, group-kill, absolute-path npx resolution) + `src-tauri/src/kernel.rs` (worktree isolation, positive whitelist + PROTECTED_PREFIXES self-protection, `validated`+`approved` flags gating `kernel_apply` so the walls are structural IN RUST, source-repo timeline, recovery-boot sentinel). `src/lib/loom/kernelBuild.ts` pipeline, `src/components/chrome/KernelDiff.tsx` diff-approval card, `src/lib/loom/recovery.ts` boot-health veto (ErrorBoundary-fed). Intent `self_edit`, dev-only guard. If extending in Phase 22: the exec.rs seam is where cargo/compiler spawns go; NEVER weaken the self-protection set (every safety file must stay non-editable — the enumerated test in kernel.rs is the guard); recovery boot must always win before suspect code loads.

**Phase 22 — Marrow (SHIPPED 2026-08-28).** LOOM edits its own Rust core in dev. `scripts/kernel-preboot.mjs` (pre-compile recovery guard, wired in `beforeDevCommand`), `src-tauri/src/kernel.rs` (whitelist extended to `src-tauri/src/**/*.rs`; `PROTECTED_RUST` + basenames; cargo validation; `preboot_heal` + sentinel mirror + `armedBy` state machine), `src-tauri/src/lib.rs` (pre-`main` heal). `KernelDiff.tsx` "restart to load" core framing; `prompts.ts` conditional Rust self-edit contract. If extending: NEVER let a safety file become editable (the enumerated test is the guard); NEVER execute untrusted generated tests (`--no-run`); the guard/pre-main state machine is intricate — round-1 review caught it rolling back GOOD edits, so re-derive the `applied`/`booting`/`armedBy` transitions carefully before touching it.

**Phase 23 — the next horizon (not started).** Packaged-app self-rebuild: a packaged app has no compiler, so self-edit beyond dev needs a supervised `npm run build`/`tauri build` + binary swap + relaunch (the deleted `agora.rs` is the spawn-hardening reference: fixed argv, canonicalized paths, process-group kill), plus macOS signed-binary + Windows in-use-lock handling and recovery-on-failure. Big, platform-specific, high-risk — its own phase. Also: faster cargo validation (shared `CARGO_TARGET_DIR` vs path-escape surface), a setup-path smoke test to shrink the untested-panic residual, a sandboxed validation for untrusted test execution, multi-file edits, and a "what LOOM changed about itself" self-commit lens. See `docs/FOLLOWUPS.md` Phase-23 backlog. Spec tightly, review adversarially (twice — that's what caught the structural gaps in 21 AND 22).

## How this codebase is worked (the house rhythm — keep it)

1. Spec → `docs/superpowers/specs/YYYY-MM-DD-<name>-design.md`; plan with checkbox tasks → `docs/superpowers/plans/`. Commit docs first on a fresh branch named for the phase.
2. One subagent per task, one commit per task, `npm run check` fully green per commit. Tests first. Tokens only (no raw hex outside `public/brand/`). Screenshot-gate UI work headlessly (vite + Playwright, mocked Tauri/routes) — **and eyeball the screenshots yourself; agents have claimed "woven cloth" while shipping graph paper.**
3. Final whole-branch review by a code-reviewer agent with phase-specific attack surface listed. Every review this run found real bugs (symlink escapes, poll leaks, permission bypasses). Fix all before merge.
4. Ship: README phase section + roadmap row (voice per BRAND.md), FOLLOWUPS prune/backlog, merge to `main`, push branch + main.
5. Memory dir (`~/.claude/projects/-Users-connorevans-Downloads-LOOM/memory/`) holds project state — update it when phases land.

## Sharp edges to remember

- Yahoo needs a browser UA or it 429s; Coinbase + Frankfurter are keyless and CORS-open; Stooq is dead; Binance.com is geo-blocked (451).
- Organs share the shell's JS realm: powers are honesty-enforcement + owner consent, NOT a security sandbox (comment in `api.ts`; FOLLOWUPS notes the real-isolation backlog item). Don't claim otherwise in docs.
- Settings migrations: retired keys go in `RETIRED_KEYS` (`src/lib/voice/settings.ts`); `migrateSettings()` runs synchronously at the top of Shell's body BEFORE the useState initializers — keep that ordering.
- Seeds install only when missing — existing installs don't get seed updates without a reset (known, in FOLLOWUPS).
- `docs/superpowers/plans/2026-08-22-vigor.md` Task 3's checkbox and the spec/plan checkboxes generally aren't ticked retroactively — the README roadmap table is the source of truth for what shipped.
