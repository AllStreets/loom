# Phase 16 Implementation Plan — Identity

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Checkbox steps. Spec: `docs/superpowers/specs/2026-08-20-identity-design.md` (authoritative).

**Goal:** one brand system on every surface (glyph, favicon, boot, chrome, BRAND.md); the constellation replaced by the Tapestry (LOOM's history woven as an interactive band); the Shuttle (Cmd+K palette + command catalog shared with voice).

**Tech stack:** existing only. No new deps.

## Global constraints
- Design language + screenshot gate (`2026-08-18-cockpit-stage3.md`) bind all UI. Tokens only; no raw hex in shell chrome (glyph SVG files may carry the canonical hex values — they ARE the brand definition).
- Gates: `npm run check` fully green per task.
- The orb never moves. Chrome z-order above decks holds.

---

### Task 1: Brand system

**Files:** `public/brand/loom-glyph.svg` + `public/favicon.svg` (new; delete `public/vite.svg`, `public/tauri.svg`); `index.html` (favicon link, meta description, theme-color `#060b18`); `src/components/Shell.tsx` (top-bar glyph beside wordmark, mood-glow treatment shared); ignition/boot surface (find it — grep ignition/first-run in Shell/ambient): glyph weft-draw animation, reduced-motion static; Settings organ ABOUT strip (glyph + name + "a computer that weaves itself" + version from package.json via existing plumbing or literal); `docs/BRAND.md` (name story, voice/copy law with examples from existing failure copy, token palette table, glyph usage); README: link BRAND.md; FOLLOWUPS: app-icon raster follow-up.

- [ ] glyph + favicon → index.html → Shell top bar → ignition → Settings ABOUT → BRAND.md → screenshots (top bar, ignition, settings) → green → commit `feat(identity): one mark on every surface — the LOOM brand system`.

---

### Task 2: The Tapestry

**Files:** DELETE `src/components/Constellation.tsx` + `.test.tsx`; remove `cockpit.constellation` from settings (+ boot migration deleting stored key — follow the tombstone/migration pattern from stage 4a); NEW `src/lib/tapestry/weave.ts` (+ tests): pure `weaveModel(inputs) -> {warp: Thread[], weft: Thread[]}` from `{commits, organs, deletedOrganIds, experiences, learnedTop, decksUsed}` with deterministic layout (no Math.random — seed from ids); NEW `src/components/Tapestry.tsx` (+ tests): SVG band low behind orb (grep Constellation's mount point in Shell and take it over, but the band is horizontal behind the orb band, not a ring); hover label (thread kind · name · age), click → organ focus (windowRegistry/open path) or timeline panel open; slow shimmer only, reduced-motion static; empty state faint warp + copy "your tapestry begins when LOOM weaves its first organ."; setting `cockpit.tapestry` on/off default on, live-toggled via `loom-settings-changed`. Data sources: `timelineLog` (core.ts), organ registry/list, `loom.organs.deleted` tombstones, experience records (`src/lib/loom/experience.ts`), `topWeights` (watch/learned).

- [ ] weave.ts tests → weave.ts → Tapestry component tests → component → Shell mount + setting + constellation removal/migration → screenshots (populated + empty + reduced-motion) → green → commit `feat(identity): the constellation dies, the tapestry lives — LOOM's history woven`.

---

### Task 3: The Shuttle

**Files:** NEW `src/lib/shuttle/catalog.ts` (+ tests): `COMMANDS: CatalogEntry[]` — `{id, phrase, aliases, hint, group: "decks"|"watch"|"build"|"organs"|"system", kind}` derived from the real deck rules (`src/lib/decks/commands.ts` — export whatever pattern table exists rather than duplicating strings; refactor that file minimally so patterns are data) + watch/briefing intents + build/organ verbs; `fuzzyFilter(q, entries)` pure + tested. NEW `src/components/Shuttle.tsx` (+ tests): Cmd+K/Ctrl+K opens glass overlay (input, grouped list, fuzzy filter, arrow-key selection, Enter executes, Esc closes; focus trapped; opening while deck interact-mode still works — listener on window capture); execution routes through the SAME path voice transcripts take (find where voice hands text to the companion runtime — `handleUtterance` or equivalent in Shell — and call exactly that); free text with no match falls through to companion chat send; orb pulse on execute (existing event). Voice "help"/"what can you do" intent speaks group names + 2 examples each from the catalog (companion runtime, no model call).

- [ ] catalog tests → catalog (+ commands.ts data refactor) → fuzzy → Shuttle tests → Shuttle → Shell wiring + help intent → screenshots (palette open, filtered) → green → commit `feat(identity): the shuttle — one grammar for voice and hand`.

---

### Task 4: Ship

README Phase 16 section + roadmap row; FOLLOWUPS prune/backlog; final whole-branch review (special attention: settings migration correctness, Shuttle keyboard capture vs deck iframes and PTT Space key, tapestry perf with many commits); fixes; merge → push.

- [ ] docs → review → fixes → merge → push → commit `feat(identity): phase 16 ships — identity`.

## Self-review notes
- The catalog refactor of `decks/commands.ts` is the riskiest edit (regression surface = every voice command); the existing tests there must keep passing untouched wherever possible.
- Cmd+K must not fight the deck iframes: listener at window capture phase in the shell document; iframes can't see it when chrome has focus — verify with interact-on.
- Tapestry reads history at mount + on `loom-fleet-activity`/organ events; no polling loop.
