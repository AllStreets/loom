# Phase 18 Implementation Plan — Excision

> **For agentic workers:** checkbox steps. Owner verdict (2026-08-22): AGORA is removed from LOOM entirely; the native floor (Coinbase ladder + tape, no AGORA dependency) folds into the Terminal as the crypto detail view.

**Goal:** no trace of AGORA remains — Rust spawn subsystem, deck, settings, voice phrases, docs. The Terminal absorbs the floor: clicking a crypto-strip symbol opens a floor overlay (ladder + tape + spot) the way clicking an equity opens the chart overlay. LOOM carries four decks: void, globe, terminal, ember.

## Task 1: Excise AGORA

- **Rust:** delete `src-tauri/src/agora.rs`; remove module/commands from `lib.rs` plus the `RunEvent::ExitRequested`/`Exit` kill hooks; drop the `libc` dependency if agora.rs was its only consumer (verify by grep before removing from Cargo.toml).
- **TS:** delete `src/components/decks/AgoraDeck.tsx` + test; remove agora wrappers from `core.ts` (+ tests); remove `"agora"` from the DeckId union, DeckLayer, Shell SegBtns; retire settings `deck.agora.url`, `deck.agora.path`, `deck.agora.product` via the existing `RETIRED_KEYS` migration (idempotent, tested) and remove their Settings-organ fields.
- **Voice/catalog:** remove agora phrases from `decks/commands.ts` tables and few-shot examples; the Shuttle catalog follows automatically (verify by test). "open the floor" / "show the floor" become Terminal aliases (continuity — the floor lives there now).
- **Docs:** grep README + FOLLOWUPS for agora/AGORA; edit every present-tense claim honestly (historical stage sections may note "removed in Phase 18" rather than being rewritten); drop AGORA-specific backlog items (db:setup, engine host, command bridge, live-iframe screenshot); keep floor backlog items reworded for the Terminal.

- [ ] rust excision → ts excision → settings migration → voice/catalog → docs → green → commit `feat(excision): agora leaves the ship — four decks, no strays`.

## Task 2: The floor folds into the Terminal

- `TerminalDeck.tsx`: clicking a CRYPTO-strip symbol opens a floor overlay (same glass overlay pattern/testids style as the symbol detail): spot + 24h Δ% header, order-book ladder (12/side, cumulative bars, mid + spread bps, 2s poll), trades tape (taker-tinted, 3s poll), Esc/click-out/✕ closes, polls stop on close (fake-timer proven). Reuse the floor components from the deleted AgoraDeck by extracting them to `src/components/terminal/Floor.tsx` (+ test) BEFORE deleting — preserve the pure helpers (takerSide, ladder math) and their tests.
- Poll lifecycle: overlay-only (no background floor polls); document-hidden pause via `usePoll`.
- Screenshots: crypto floor overlay open (populated), error state.

- [ ] extract Floor → wire crypto-strip click → lifecycle tests → screenshots → green → commit `feat(excision): the floor finds its home — crypto depth inside the terminal`.

## Task 3: Ship

README Phase 18 section + roadmap row; FOLLOWUPS resolved section; review (attention: no dangling agora references — grep is the gate; settings migration; overlay poll lifecycle); fixes → merge → push.

- [ ] docs → review → fixes → merge → push → commit `feat(excision): phase 18 ships — excision`.
