# dependencies
node_modules/

# secrets — never commit. js/keys.js is now a SAFE, secret-free committed file
# that reads window.AUSPEX_KEYS; the REAL keys live in js/keys.local.js (below).
js/keys.local.js
.env
.env.*
worker/.env
!worker/.env.example

# brainstorm companion (local only)
.superpowers/

# os
.DS_Store
**/.DS_Store

# live snapshot (generated by the worker)
/snapshot.json

# playwright scratch
.playwright-mcp/

# verification screenshots (local only)
auspex-*.png
page-*.png
Screenshot*.png
